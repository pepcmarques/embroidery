generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
}

model Subscriber {
  id       Int     @id @default(autoincrement())
  email    String  @unique
  isActive Boolean @default(true)
}
